function setup() {
  createCanvas(1000, 1000);
  background(255);
}

function draw() {
  for (var i = 0; i < 500; i = i + 10) {
    line(500 - i, i, i + 500, i);
  }
  for (var x = 0; x < 500; x = x + 10) {
    line(x, x + 500, 1000 - x, x + 500);
  }
}
